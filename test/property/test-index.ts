import { testProp } from 'ava-fast-check'

/**
 * Library under test
 */

//import { index } from 'index'

testProp.skip(
    'TODO: property-test yaii',
    [
        // arbitraries
    ],
    () =>
        // test arguments
        {
            // return assertion

            return true
        }
)
